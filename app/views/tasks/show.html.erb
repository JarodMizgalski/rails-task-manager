<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "tasks", tasks_path %>
<%= link_to "edit tasks", edit_task_path %>

<%= link_to "delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "delete #{@task.title}?"} %>
