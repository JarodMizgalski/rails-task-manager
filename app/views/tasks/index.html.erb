<h1>To do List</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Create new task", new_task_path %>

<%# if statement for checkboxes if true check if not no check %>
